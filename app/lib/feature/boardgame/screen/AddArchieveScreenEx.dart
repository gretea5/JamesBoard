import 'package:flutter/material.dart';

class AddArchiveScreenEx extends StatelessWidget {
  const AddArchiveScreenEx({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text("AddArchiveScreenEx", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
