import 'package:flutter/material.dart';

class HomeScreenEx extends StatelessWidget {
  const HomeScreenEx({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("HomeScreenEx"));
  }
}