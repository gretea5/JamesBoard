import 'package:flutter/material.dart';

class MyPageScreenEx extends StatelessWidget {
  const MyPageScreenEx({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("MyPageScreenEx"));
  }
}