import 'package:flutter/material.dart';

class RecommGameScreenEx extends StatelessWidget {
  const RecommGameScreenEx({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("RecommGameScreenEx"));
  }
}